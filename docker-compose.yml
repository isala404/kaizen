services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    ports:
      - "9081:9081"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgres://postgres:forge@db:5432/kaizen
      - SQLX_OFFLINE=true
      - FORGE_OTEL_ENABLED=true
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel:4318
    working_dir: /app
    command: cargo watch --poll -x "run --no-default-features"
    volumes:
      - .:/app
      - cargo_cache:/usr/local/cargo/registry
      - ./target:/app/target
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9081/_api/ready"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 60s

  db:
    image: postgres:18
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d kaizen"]
      interval: 5s
      timeout: 5s
      retries: 5

  otel:
    image: grafana/otel-lgtm:0.22.1
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - POSTGRES_HOST=db

volumes:
  postgres_data:
  cargo_cache:
